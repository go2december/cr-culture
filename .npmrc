legacy-peer-deps=true

# Allow install scripts for native packages (npm 11+ allow-scripts whitelist)
allow-scripts[esbuild]=true
allow-scripts[unrs-resolver]=true
# @img/sharp-linux-musl-x64 is a pre-built binary — no install scripts needed
